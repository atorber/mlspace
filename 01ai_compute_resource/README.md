# 资源管理模块

本模块对应百度百舸 AI 计算平台中的 AI 计算资源模块，主要承载 `ResourcePool`、`Queue` 以及资源接入和运行时访问模式相关资料。

## 包含内容

- `resource_pool/`：资源池相关资料，当前包含 `k8s_runtime/` 等运行时访问模式
- `queue/`：队列相关资料、使用说明与调度约定

## 边界说明

本模块负责训练或服务运行时所依赖的资源接入方式。
模型导出和服务兼容性归 `serving/`，镜像构建归 `ai_asset/image/`。
