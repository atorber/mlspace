# 集成模块

本目录作为横向集成资料目录，用于承接百舸平台相关的 SDK、OpenAPI、外部集成样例和跨模块工具链资料。

## 当前结构

- `tool/`：常用工具资料与辅助脚本说明
- `sdk/`：SDK 资料与集成样例，当前包含 `aihc-python-sdk/`
- `openapi/`：OpenAPI 说明、接口约定与调用示例

## 放置规则

当一套内容同时服务多个功能模块，且更适合作为平台外围集成能力呈现时，优先放在本目录 `references/` 下的对应子目录。
如果某个集成已经只服务单一模块，再考虑回收进对应功能域。
