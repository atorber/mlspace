# 训练模块

本模块对应百度百舸 AI 计算平台中的分布式训练 `Job` 模块，沉淀训练模板、训练约定和训练实践资料。

## 包含内容

- `pytorch_standard/`：常规训练代码的解耦工程结构
- `dist_training/`：多机多卡训练的启动、日志与恢复约定

## 边界说明

训练模块负责训练工程组织、训练任务启动和训练过程治理。
部署前的模型导出与推理兼容检查已经拆分到 `serving/compatibility/`，不再放在训练域中。
